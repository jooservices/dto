services:
  php:
    build:
      context: .
      dockerfile: Dockerfile
    image: jooservices/dto:php85
    working_dir: /app
    volumes:
      - .:/app
    environment:
      COMPOSER_CACHE_DIR: /tmp/composer-cache
    tty: true
